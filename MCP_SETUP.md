# Meta MCP Server - Setup Guide

## 🔌 Два способи підключення

### Варіант 1: Локальний запуск (рекомендовано для розробки)

**Конфігурація:**

Додайте до конфігурації вашого MCP клієнта:

> [!IMPORTANT]
> Використовуйте **повні абсолютні шляхи** замість `~`. Тильда не розширюється в конфігураціях MCP клієнтів!

```json
{
    "mcpServers": {
        "meta-mcp-server": {
            "command": "/home/[USERNAME]/projects/MetaMCP/venv/bin/meta-mcp-server",
            "args": [],
            "cwd": "/home/[USERNAME]/projects/MetaMCP"
        }
    }
}
```

**Переваги:**
- ✅ Швидший запуск
- ✅ Простіше для debugging
- ✅ Прямий доступ до `.env`

---

### Варіант 2: Docker (рекомендовано для production)

**1. Переконайтесь що Docker image створено:**
```bash
cd /home/[USERNAME]/projects/MetaMCP
docker build -t meta-mcp .
```

**2. Конфігурація MCP клієнта:**

Додайте до конфігурації вашого MCP клієнта:
```json
{
  "mcpServers": {
    "meta-mcp-server": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "--env-file",
        "/home/[USERNAME]/projects/MetaMCP/.env",
        "meta-mcp"
      ]
    }
  }
}
```

**Переваги:**
- ✅ Ізоляція середовища
- ✅ Консистентність між системами
- ✅ Простий deployment

**Примітка:** Docker варіант може бути трохи повільнішим через overhead контейнеризації.

---

### Варіант 3: Docker Compose

**Конфігурація:**
```json
{
  "mcpServers": {
    "meta-mcp-server": {
      "command": "docker-compose",
      "args": [
        "-f",
        "/home/[USERNAME]/projects/MetaMCP/docker-compose.yml",
        "run",
        "--rm",
        "meta-mcp-server"
      ],
      "cwd": "/home/[USERNAME]/projects/MetaMCP"
    }
  }
}
```

---

## 🚀 Як вибрати?

| Критерій | Локальний | Docker |
|----------|-----------|--------|
| Швидкість запуску | ⚡ Швидко | 🐢 Повільніше |
| Debugging | ✅ Легко | ⚠️ Складніше |
| Безпека/Ізоляція | ⚠️ Немає | ✅ Повна |
| Простота setup | ✅ Проста | ⚠️ Потрібен Docker |
| Production ready | ⚠️ Залежить від системи | ✅ Так |

- Для розробки та тестування → **Локальний**
- Для production/deployment → **Docker**

---

## ✅ Після налаштування

1. **Перезапустіть ваш MCP клієнт**
2. Перевірте статус MCP серверів у вашому клієнті
3. Спробуйте використати доступні інструменти:
   - "Send a message to my Facebook Page"
   - "Post something on Facebook"
   - "Get analytics from my Page"

---

## 🔍 Перевірка роботи

**Тест вручну (Docker):**
```bash
cd /home/[USERNAME]/projects/MetaMCP
docker run --rm -i --env-file .env meta-mcp
```

Потім відправте JSON-RPC:
```json
{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"test","version":"1.0"}}}
```

**Тест вручну (Локально):**
```bash
source venv/bin/activate
meta-mcp-server
```

---

## 🛠️ Вирішення проблем (Troubleshooting)

### 🔴 Помилка "403 Forbidden" при пості
Якщо ви отримуєте помилку при спробі зробити пост, ймовірно ви використовуєте **User Token** замість **Page Token**.
User Token не дозволяє постити на сторінки без спеціальних дозволів.

**Рішення:**
Запустіть скрипт для отримання правильного токена:
```bash
python scripts/get_page_token.py
```
Він покаже список ваших сторінок і дасть правильний `FACEBOOK_PAGE_ACCESS_TOKEN`.

### 🔴 Помилка "Session has expired" або "could not be decrypted"
Це означає, що ваш токен застарів або був скопійований з помилкою (зайві пробіли, неповна стрічка).
1. Згенеруйте новий токен.
2. Оновіть `.env`.
3. **Обов'язково перезапустіть сервер** (перезавантажте Docker контейнер або клієнт).
